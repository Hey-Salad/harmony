/**
 * Standalone Universe Dashboard - No Firebase, No Auth
 * This is a completely independent entry point for the 3D visualization
 */

import UniverseDashboard from './dashboard/UniverseDashboard';

export default function UniverseStandalone() {
  return <UniverseDashboard />;
}
